const JWT_ACCESS_TOKEN = process.env.NEXT_

const getTokenSecondsRemaining = (token: string): number => {
    if (!token) return 0;
    try {
        const tokenPayload = 
        
    } catch (error) {
        
    }
}