const JWT_ACCESS_TOKEN = process.env.NEXT_PUBLIC_JWT_ACCESS_TOKEN;

const getTokenSecondsRemaining = (token: string): number => {
    if (!token) return 0;
    try {
        const tokenPayload = 
        
    } catch (error) {
        
    }
}